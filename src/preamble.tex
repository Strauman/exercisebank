%!TEX root = main.tex
\makeatletter
\@ifundefined{ownLineNoSpacesGotIt}{
% \edef\warn{Compilation wont work unless \string\end{problem} and \string\end{solution} are on their own lines and without any spaces. This warning can be removed by doing \string\def\string\ownLineNoSpacesGotIt{} before \string\usepackage{exbank}}
\@latex@warning{Compilation wont work unless \string\end{problem} and \string\end{solution} are on their own lines and without any spaces. This warning can be removed by doing \string\def\string\ownLineNoSpacesGotIt{} before \string\usepackage{exbank}}
}{}
\@ifundefined{exercisesDir}{
\gdef\exercisesDir{exercises}
}{}
\makeatother


\global\let\incl = \input
\input{packages}
\input{i18n}
\input{at}
\input{problemstyle}
\input{envcontrol}
\input{isin}
\input{setbuilder}
\input{pplabel}
